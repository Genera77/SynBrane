# Server host/port
PORT=3001
HOST=0.0.0.0

# Base pitch used for tuning calculations (Hz)
BASE_FREQUENCY=440

# Directory paths
SCALES_DIR=./scales
RENDER_OUTPUT_DIR=./renders

# Offline render sample rate (Hz)
RENDER_SAMPLE_RATE=44100

# SuperCollider bridge toggle (fallback to Node DSP when false)
SUPER_COLLIDER_ENABLED=false
# Optional override for sclang executable path
SUPER_COLLIDER_SCLANG_PATH=sclang
