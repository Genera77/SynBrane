# Server host/port
PORT=3000
HOST=0.0.0.0

# Base pitch used for tuning calculations
BASE_FREQUENCY=440

# Directory paths
SCALES_DIR=./scales
RENDER_OUTPUT_DIR=./renders

# API base URL override (useful when proxying)
API_BASE_URL=http://localhost:3000

# SuperCollider bridge configuration (placeholder for OSC/NRT render)
SUPER_COLLIDER_HOST=127.0.0.1
SUPER_COLLIDER_PORT=57110
SUPER_COLLIDER_SCLANG_PATH=sclang

# Offline render sample rate
RENDER_SAMPLE_RATE=44100
